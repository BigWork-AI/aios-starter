# Nothing private belongs in this repository; the private folder lives outside it.
.env
*.env
.DS_Store
inbox/quarantine/*
!inbox/quarantine/README.md
# local stamps, never shared
memory/receipts/upgrade-check.json
