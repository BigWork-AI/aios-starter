# Clients, prospects and partners

One folder per business, created by `/client` from `.aios/templates/client/`. `meta.yml` is where things stand now; `ledger.md` is what happened; `memory/` holds durable facts.
