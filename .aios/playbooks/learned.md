# /learned — capture what this session taught

1. Reread the whole session. List every decision, fact, state change, dated promise, correction and lesson.
2. Screen against `company/access.md`. Private items: "not saved here".
3. Route each item to its one home: present state to `company/` or the client's `meta.yml`; dated history to the client's `ledger.md`; facts to `memory/facts.md` files with source, date and status; company-wide decisions to `memory/decisions.md`; dated promises to `memory/follow-ups.md`; a changed method to the skill or playbook it belongs to.
4. Corrections: fix present-state files directly; in dated files add a new dated correction row and leave the original.
5. Nothing worth keeping: say so and stop. Do not write an empty note.
6. Report: one line on what was saved, then file → what went in, then anything not saved and why. Then save (`sh .aios/hooks/autosave.sh`).
