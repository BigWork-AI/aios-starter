# Claude entry point

Read and follow @AGENTS.md before work. It is the only policy authority in this brain. Commands in `.claude/commands/` point at the playbooks in `.aios/playbooks/`. Run `./scripts/setup.sh` before edits.
